# remove-join-message

Removes the player join and leave messages from Fabric servers.

The removed messages are:
  - `multiplayer.player.joined`: "{player} joined the game"
  - `multiplayer.player.joined.renamed`: "{player} (formerly known as {name}) joined the game"
  - `multiplayer.player.left`: "{player} left the game"
